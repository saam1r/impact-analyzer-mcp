# Impact Analyzer MCP - Environment Variables Example
# Copy these to your ~/.cursor/mcp.json env section or set in your shell profile

# Default base branch to compare against (default: master)
DEFAULT_BASE_BRANCH=master

# Maximum file size to read in bytes (default: 10000)
MAX_FILE_SIZE=10000

# Maximum diff size in bytes (default: 50000)
MAX_DIFF_SIZE=50000

# Test directories to scan (comma-separated, no spaces after commas)
TEST_DIRECTORIES=tests/e2e/specs,tests/e2e/api/api-specs,tests/unit/backend,tests/integration/backend

# Maximum number of files to read content from (default: 10)
MAX_FILES_TO_READ=10

# Maximum number of commits to include (default: 10)
MAX_COMMITS=10

# Maximum depth for indirect dependency analysis (default: 2)
# Higher values may be slower but find more dependencies
MAX_DEPENDENCY_DEPTH=2
